"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var RejectionLearningService_1;
Object.defineProperty(exports, "__esModule", { value: true });
exports.RejectionLearningService = void 0;
const common_1 = require("@nestjs/common");
const prisma_service_1 = require("../prisma/prisma.service");
const client_1 = require("@prisma/client");
const MIN_SAMPLES_FOR_PATTERN = 5;
const MIN_CONFIDENCE_FOR_RULE = 0.7;
let RejectionLearningService = RejectionLearningService_1 = class RejectionLearningService {
    constructor(prisma) {
        this.prisma = prisma;
        this.logger = new common_1.Logger(RejectionLearningService_1.name);
    }
    async analyzePatterns() {
        const rejections = await this.prisma.candidateFeedback.findMany({
            where: {
                action: client_1.FeedbackAction.REJECT,
                snapshot: { not: client_1.Prisma.DbNull },
            },
            select: { snapshot: true },
        });
        if (rejections.length < MIN_SAMPLES_FOR_PATTERN) {
            this.logger.log(`Not enough rejections for pattern analysis (${rejections.length}/${MIN_SAMPLES_FOR_PATTERN})`);
            return [];
        }
        const snapshots = rejections
            .map((r) => r.snapshot)
            .filter((s) => s !== null);
        const patterns = [];
        const scorePattern = this.analyzeNumericThreshold(snapshots, 'totalScore', 'Score threshold');
        if (scorePattern)
            patterns.push(scorePattern);
        const followerPattern = this.analyzeNumericThreshold(snapshots, 'followers', 'Follower threshold');
        if (followerPattern)
            patterns.push(followerPattern);
        const repoPattern = this.analyzeNumericThreshold(snapshots, 'publicRepos', 'Repository count threshold');
        if (repoPattern)
            patterns.push(repoPattern);
        const commitPattern = this.analyzeNumericThreshold(snapshots, 'totalCommits', 'Commit count threshold');
        if (commitPattern)
            patterns.push(commitPattern);
        const languagePattern = this.analyzeCategoricalFrequency(snapshots, 'primaryLanguage', 'Common rejected language');
        if (languagePattern)
            patterns.push(languagePattern);
        const sourcePattern = this.analyzeArrayFrequency(snapshots, 'sources', 'Common rejected source');
        if (sourcePattern)
            patterns.push(sourcePattern);
        this.logger.log(`Analyzed ${snapshots.length} rejections, found ${patterns.length} patterns`);
        return patterns.sort((a, b) => b.confidence - a.confidence);
    }
    async generateRulesFromPatterns() {
        const patterns = await this.analyzePatterns();
        const highConfidencePatterns = patterns.filter((p) => p.confidence >= MIN_CONFIDENCE_FOR_RULE);
        let created = 0;
        for (const pattern of highConfidencePatterns) {
            const existingRule = await this.prisma.rejectionRule.findFirst({
                where: {
                    autoGenerated: true,
                    name: { contains: pattern.field },
                },
            });
            if (existingRule) {
                await this.prisma.rejectionRule.update({
                    where: { id: existingRule.id },
                    data: {
                        conditions: [
                            {
                                field: pattern.field,
                                operator: pattern.operator,
                                value: pattern.value,
                            },
                        ],
                        confidence: pattern.confidence,
                        hitCount: pattern.hitCount,
                    },
                });
            }
            else {
                await this.prisma.rejectionRule.create({
                    data: {
                        name: `Auto: ${pattern.description}`,
                        description: `Automatically generated rule based on ${pattern.hitCount} rejections`,
                        conditions: [
                            {
                                field: pattern.field,
                                operator: pattern.operator,
                                value: pattern.value,
                            },
                        ],
                        confidence: pattern.confidence,
                        hitCount: pattern.hitCount,
                        autoGenerated: true,
                        enabled: pattern.confidence >= 0.8,
                    },
                });
                created++;
            }
        }
        this.logger.log(`Generated ${created} new rules from ${highConfidencePatterns.length} high-confidence patterns`);
        return created;
    }
    analyzeNumericThreshold(snapshots, field, description) {
        const values = snapshots
            .map((s) => s[field])
            .filter((v) => v !== null && typeof v === 'number');
        if (values.length < MIN_SAMPLES_FOR_PATTERN) {
            return null;
        }
        values.sort((a, b) => a - b);
        const percentile25 = values[Math.floor(values.length * 0.25)];
        const percentile75 = values[Math.floor(values.length * 0.75)];
        const median = values[Math.floor(values.length * 0.5)];
        const belowMedianCount = values.filter((v) => v <= median).length;
        const belowMedianRatio = belowMedianCount / values.length;
        if (belowMedianRatio >= 0.6) {
            const threshold = percentile75;
            const confidence = Math.min(belowMedianRatio, 0.95);
            return {
                field,
                operator: '<',
                value: Math.round(threshold),
                confidence,
                hitCount: belowMedianCount,
                description: `${description} < ${Math.round(threshold)}`,
            };
        }
        return null;
    }
    analyzeCategoricalFrequency(snapshots, field, description) {
        const values = snapshots
            .map((s) => s[field])
            .filter((v) => v !== null && typeof v === 'string');
        if (values.length < MIN_SAMPLES_FOR_PATTERN) {
            return null;
        }
        const frequency = new Map();
        for (const value of values) {
            frequency.set(value, (frequency.get(value) || 0) + 1);
        }
        const sortedEntries = [...frequency.entries()].sort((a, b) => b[1] - a[1]);
        if (sortedEntries.length === 0) {
            return null;
        }
        const [topValue, topCount] = sortedEntries[0];
        const ratio = topCount / values.length;
        if (ratio >= 0.3 && topCount >= MIN_SAMPLES_FOR_PATTERN) {
            return {
                field,
                operator: '=',
                value: topValue,
                confidence: Math.min(ratio + 0.2, 0.95),
                hitCount: topCount,
                description: `${description}: ${topValue}`,
            };
        }
        return null;
    }
    analyzeArrayFrequency(snapshots, field, description) {
        const allValues = [];
        for (const snapshot of snapshots) {
            const value = snapshot[field];
            if (Array.isArray(value)) {
                allValues.push(...value);
            }
        }
        if (allValues.length < MIN_SAMPLES_FOR_PATTERN) {
            return null;
        }
        const frequency = new Map();
        for (const value of allValues) {
            frequency.set(value, (frequency.get(value) || 0) + 1);
        }
        const sortedEntries = [...frequency.entries()].sort((a, b) => b[1] - a[1]);
        if (sortedEntries.length === 0) {
            return null;
        }
        const [topValue, topCount] = sortedEntries[0];
        const ratio = topCount / snapshots.length;
        if (ratio >= 0.4 && topCount >= MIN_SAMPLES_FOR_PATTERN) {
            return {
                field,
                operator: 'in',
                value: [topValue],
                confidence: Math.min(ratio, 0.95),
                hitCount: topCount,
                description: `${description}: ${topValue}`,
            };
        }
        return null;
    }
};
exports.RejectionLearningService = RejectionLearningService;
exports.RejectionLearningService = RejectionLearningService = RejectionLearningService_1 = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [prisma_service_1.PrismaService])
], RejectionLearningService);
//# sourceMappingURL=rejection-learning.service.js.map